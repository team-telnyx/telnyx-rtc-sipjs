{
  "name": "telnyx-rtc-sipjs",
  "private": true,
  "version": "0.0.0",
  "description": "Telnyx SIP.js helpers monorepo",
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "test": "yarn workspaces foreach -pt run test",
    "typecheck": "yarn workspaces foreach -pt run typecheck",
    "clean": "yarn workspaces foreach -pt run clean || true"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/team-telnyx/telnyx-rtc-sipjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "yarn@4.1.1"
}
